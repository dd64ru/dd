@{
    if (!IsPost)
    {
        Response.SetStatus(HttpStatusCode.NotFound);
        return;
    }

    string id = Request["id"];

    if (!PersonalBlog.WebUI.LogicProvider.UserLogic.Delete(id))
    {
        Response.SetStatus(HttpStatusCode.BadRequest);
        return;
    }
}